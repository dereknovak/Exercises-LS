def multiply(num1, num2)
  num1 * num2
end

multiply(5, 3)

=begin

On line 5, the defined method `multiply` is invoked. The integers `5` and `3`
are passed in as arguments and are bound to the method's parameters `num1` and
`num2`, respectively. Within the method, `num1` and `num2` are multiplied
together, returning 15 from the method invocation.

=end